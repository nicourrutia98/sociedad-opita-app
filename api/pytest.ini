[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    slow: marca tests lentos (>5s)
    integration: requiere red real o AWS real
    aws: requiere credenciales AWS
